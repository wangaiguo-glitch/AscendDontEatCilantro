# 文件组织：
object_detect yolov8s目标检测模型

depth_estimation midas 深度估计模型

VLM 云端多模态大模型

voice 语音播报

README.md 运行命令

运行命令
github提交代码：
```bash
git status
git add .
git commit -m "更改说明"
git push origin main //提交到仓库
```